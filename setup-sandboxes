cabal sandbox init --sandbox=../.cabal-sandbox

MODULES="
oculus-mini 
freetype-gl-mini 
gl-pal 
glfw-pal
pd-haskell
hydra-hs"

for module in $MODULES; do
  cabal sandbox add-source ../$module
  (cd ../$module && cabal sandbox init --sandbox=../.cabal-sandbox)
done
